import React from 'react';
import { render, fireEvent } from '@testing-library/react-native';
import Tip from '../../components/Tip';

describe('Tip component', () => {
    test('appears when called', () => {
        expect(true).toEqual(true);

    });
})